var app = angular.module(appname);

app.controllerProvider.register('classController', ['$scope', '$resource', '$compile', '$state', 'token', function ($scope, $resource, $compile, $state, token) {

   }]);